#include <cctype>
#include <iostream>
#include <string>
using namespace std;

bool isPalindromeR(string userText, int iteration);

bool isPalindrome(string userText, bool caseFlag, bool spaceFlag);

void printUsage(string programName);
